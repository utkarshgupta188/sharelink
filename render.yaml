services:
  - type: web
    name: p2p-file-sharing
    env: node
    buildCommand: npm ci
    startCommand: node app.js
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: /api/health
    autoDeploy: true
